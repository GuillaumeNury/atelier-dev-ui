/**
 * On peut exporter des types / constantes / fonctions / classes pour s'en servir dans d'autres fichiers
 */
export function add(a: number, b: number): number {
  return a + b;
}

export const PI = 3.14;
